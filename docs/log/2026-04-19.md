# 开发日志 — 2026-04-19

## saved-lineup-enhancements 后续

### 1. 排阵导入修复（已部署）
**提交：** `3f75465`、`da784b7`、`e5f4a78`、`fbcd82e`、`5406848`、`102f068`

- Import 时按球员**姓名**映射源 ID → 目标队伍 ID，修复"上场女性球员少于2人"误报
- `getLineupsByTeam()` 自愈：已有导入的排阵在读取时按姓名解析未知 ID
- 清理 git：移除 `backend/data/tennis-data.json`（runtime data），`docs/import/*.csv`（含球员 PII）加入 gitignore
- 提交 scripts/ZJ2026 导入工具和 fly deploy 计划文档
- CLAUDE.md 入库

### 2. 去重策略改进（已部署）
**提交：** `deafe45`

- 同 8 人**不同配对**视为不同排阵（可并存）
- `saveLineup` / `updateLineup` / `importLineups` 改为按「配对组合」去重（排序后的 `playerA+playerB` 集合）
- 排阵**生成**保留原"8 人集合"去重（确保 6 个候选人员不同）

### 3. BatchImportServiceTest 预存在失败修复
**提交：** `102f068`
- 测试 mock 传了 `null` 作为 actualUtr 参数，但服务实际传 `player.getUtr()`，导致 `doThrow` 不匹配
- 修正 stub 参数匹配实际调用

### 4. Fly.io OOM 排查（已回滚）
**提交：** `f6c3c19`
- 本以为 `-Xmx384m` 在 256MB 容器里是问题，尝试减堆 → 用户反馈"以前没改时能用"，还原 `JAVA_TOOL_OPTIONS = "-Dfile.encoding=UTF-8"`，让 JVM 使用容器感知默认
- 应用层 `MAX_OPTIONAL_POOL=18` 等内存优化保留

---

## mobile-ui-optimization（进行中）

### 5. Brainstorming + Spec
**提交：** `5c612fe`、`d3502a2`

- 用 superpowers brainstorming 的 Visual Companion 生成浏览器可视化 mockup
- 5 个页面的 Mobile 设计（队伍管理、队伍详情、已保存排阵、排阵生成、对手分析）
- 决策：单断点 `lg` (1024px)；列表/详情 URL 驱动二选一；对手分析改纯下拉
- 写出 design spec (`docs/superpowers/specs/2026-04-19-mobile-ui-optimization-design.md`)
- 用 openspec 建 change (`openspec/changes/mobile-ui-optimization/`)：proposal + design + 6 个 capability specs + tasks
- 用 superpowers writing-plans 生成详细实施计划（1300 行带 TDD 代码）

### 6. 第一批：全局框架（后端无改动）
**提交：** `2e336bb`

- 新增 `AppHeader.vue`：汉堡菜单 + 可选返回按钮 + 标题 + actions slot；`lg:hidden`
- 重构 `MainLayout.vue`：`provide('sidebarOpen', ref(false))`；Escape 关闭抽屉
- 决定合并 `SidebarDrawer.vue` 逻辑到 MainLayout（无需单独组件）

**代码审查修复：**

| 级别 | 问题 | 修复 |
|------|------|------|
| HIGH | `inject` fallback 用普通对象（不是 ref），独立使用时静默失败 | 改为 `inject('sidebarOpen', ref(false))` |
| HIGH | 汉堡 aria-label 固定为"打开导航"无视状态 | 动态 `:aria-label` + `:aria-expanded` |
| HIGH | 返回按钮缺 aria-label + `type="button"` | 补上，form 内安全 |
| HIGH | 移除 MainLayout 的 `pt-14` 导致内容被固定头部遮挡 | 恢复 `pt-14 lg:pt-0` |
| MEDIUM | 抽屉无 Escape 键关闭（WCAG 2.1.2） | MainLayout 加全局 keydown 监听 |
| LOW | 无 `overflow-x-hidden` 可能闪现横向滚动 | 加上 |

**测试：** 381 tests 全部通过（新增 16 个 AppHeader 测试）

---

### 7. 第二批：队伍管理 + 球员卡片
**提交：** `9b72168`

- `TeamManagerView`：Mobile 根据 `route.params.id` 二选一显示列表/详情；桌面保持两栏
- `TeamDetail`：引入 AppHeader（标题=队伍名，返回→`/`）；桌面表格 `hidden lg:block` 保留；新增 `lg:hidden` 球员卡片列表
- 卡片交互：点击/Enter/Space 展开 Verified Doubles + Notes + [编辑][删除]
- A11y：`role="button"` + `tabindex="0"` + `aria-expanded` 支持键盘

**代码审查修复：**

| 级别 | 问题 | 修复 |
|------|------|------|
| HIGH | Mobile 卡片 `<div>` 只有 `@click`，键盘不可操作 | 加 `role/tabindex/aria-expanded` + keydown.enter/space 处理 |
| MEDIUM | Mobile UTR 显示原值，桌面 `.toFixed(2)` | 统一 `Number(player.utr).toFixed(2)` |
| MEDIUM | 测试未覆盖键盘操作、`click.stop`、空列表 | 补充 5 个测试 |

**测试：** 401 tests 全部通过（新增 20 个：6 TeamManagerView + 14 TeamDetail）

---

### 8. 第三批：LineupCard 每人一行布局
**提交：** `cfdf113`

- 每对 D1-D4 渲染 2 行（player1 上，player2 下）
- 每行：`[性别徽章 pink女/blue男] 姓名(加粗) UTR 实:actualUtr`
- `showPlayerUtr=false` 时退回原单行布局（兼容）
- `pairActualUtrSum` 只对非 null 的 actualUtr 求和，全为 null 时返回 null 隐藏"实:"
- `sortedPairs` 防御性排序：未知 position 排到末尾
- `min-w-0` 让长姓名正确 truncate

**代码审查修复：**

| 级别 | 问题 | 修复 |
|------|------|------|
| HIGH | `pairActualUtr` 用 `actualUtr ?? utr` 混入 scheduled UTR，误导性"实:X.XX" | 只 sum 非 null 值，全 null 时返回 null 隐藏显示 |
| HIGH | `truncate` 在 flex row 内未生效（缺 `min-w-0`） | 内层 flex row 加 `min-w-0` |
| MEDIUM | 测试缺少 partial-null actualUtr 场景 | 补充 2 个测试 |
| MEDIUM | `positionOrder.indexOf` 未知位置 → -1 排到顶部 | 防御 `-1 ? 99 : idx` |
| LOW | "TDD RED phase" 注释过期 | 改为常规注释 |

**测试：** 413 tests 全部通过（新增 12 个）

---

## 待完成

- [ ] 第四批：LineupHistoryView 接入 AppHeader + 验证单列
- [ ] 第四批：LineupHistoryView 验证 + 加 AppHeader
- [ ] 第五批：LineupGenerator 表单折叠
- [ ] 第六批：OpponentAnalysis 纯下拉重构（最大改动）
- [ ] E2E 测试 + 桌面视觉回归
- [ ] 部署
